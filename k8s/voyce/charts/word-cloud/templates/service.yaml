apiVersion: v1
kind: Service
metadata:
  name: {{ include "word-cloud.fullname" . }}
  labels:
    {{- include "word-cloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "word-cloud.selectorLabels" . | nindent 4 }}
